# Reports

This directory is kept for legacy Markdown report output from earlier demo stages.

The current final flow focuses on centralized log storage and on-demand AI analysis in the Web page. Generated `reports/*.md` files are runtime artifacts and should not be committed to Git.
